<h2><%= "Orientação do aluno #{@orientation.timeline.student.name} no dia #{@orientation.date.strftime('%d/%m/%Y')}" %></h2>
<div class="row">
  <div class="col-md-12">
    <ol class="breadcrumb">
      <%= render_breadcrumbs separator: "", :tag => :li %>
    </ol>    
  </div>
</div>
<p>Título: <%= @orientation.title %></p>
<p>Data: <%= @orientation.date.strftime('%d/%m/%Y') %></p>
<p>Concordância do acadêmico? <%= @orientation.accept ? "Sim" : "Não" %></p>
<p>Descrição: <br>
<%= @markdown.render(@orientation.description).html_safe %></p>
