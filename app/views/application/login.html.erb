<div class="container" ng-controller="LoginController as ctrl">
  <div class="row">
    <div class="col-md-12">
        <% flash.each do |key, value| %>
            <%= content_tag :div, value, class: "alert alert-#{key}" %>
        <% end %>
    </div>
  </div>
  <div class="row">
    <div class="col-sm-6 col-md-8 col-md-offset-2 text-center">
      <h1> TCC - UTFPR - Câmpus Guarapuava
      <h2 class="text-center login-title">Por favor, realize o login:</h2>
      <div class="account-wall col-md-8 col-md-offset-2">
        <form class="form-signin" action="<%= login_url %>" method="POST">
          <input type="text" name="user[login]" class="form-control" placeholder="Login" required autofocus><br>
          <input type="password" name="user[password]" class="form-control" placeholder="Senha" required><br>
          <button class="btn btn-lg btn-primary btn-block" type="submit">
            Entrar</button>
        </form>
      </div>
    </div>
  </div>
</div>
